-- Rename sequence gap placeholder: no-op migration to keep numbering contiguous.
-- Migration 011 was skipped during Sprint 2 development.
-- Migration 012 (seed_default_settings) already exists and ran.
-- This migration intentionally does nothing.
SELECT 1;
